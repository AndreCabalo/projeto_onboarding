spring:
  application:
    name: projeto_onboarding

  datasource:
    url: jdbc:mysql://localhost:3306/cart_test
    username: root
    password: root

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  profiles:
    active: test

  server:
    error:
      include-stacktrace: never